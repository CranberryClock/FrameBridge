# TCW-009 plan and decision record

1. Preserve the accepted textured reference path and inspect the existing
   Streamline preflight before integration work.
2. Verify the current local prerequisite state without exposing configured
   values or private paths.
3. Reconcile the preflight with NVIDIA's official Streamline 2.12 programming
   guides, DLSS guide, security guidance, runtime list, and SDK license.
4. Replace the browser's hardcoded scale claim with authenticated native runtime
   metadata and validate TypeScript/C++ builds and tests.
5. Run a fresh short reference baseline and record frame rate, return age,
   dimensions, drops, outstanding work, and stalls.
6. If the governed SDK and NVIDIA-provided application ID are absent, classify
   the result as B, publish a precise human checkpoint, and stop without a fake
   DLSS adapter or governed download.

Decision: step 6 applies. Both external inputs are absent. A real `slInit`,
support query, optimal-dimension query, `slEvaluateFeature`, and cleanup sequence
cannot be executed. This is an external prerequisite block, not evidence of
architectural incompatibility.
